def rm_od_index_char(str1):
    new_str=""
    for i in range(len(str1)):
       if i % 2 != 0:
           new_str = new_str + str1[i]
           print(new_str)
    
inpt_str = str(input("Enter string : "))
rm_od_index_char(inpt_str)